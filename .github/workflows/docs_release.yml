name: 'Docs (release)'
on:
  release:
    types:
      - published


jobs:
  build:
    uses: './.github/workflows/docs.yaml'
    with:
      git_commit: '${{ github.ref_name }}'
      gh_pages_dest_dir: './releases/${{ github.ref_name }}'
